import "../index.css"; // Import the CSS file

const RunClub = () => {
  return (
    <div className="container">
      <h2>Welcome to Run Clubs!</h2>
      <p>Get ready for an exciting Run Club!</p>
      <a href="/">Go back</a>
    </div>
  );
};

export default RunClub;
